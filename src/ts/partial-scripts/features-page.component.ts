import { Component } from "../classes/component.class.js";

export class FeaturesPageComponent extends Component {
    override name: string = "features-page";

    override onReady(): void {
        
    }
}